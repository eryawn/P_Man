#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <readline/readline.h>

int main(int argc, char const *argv[])
{
 /*    int num = 1;
    FILE *fptr;
    fptr = fopen("testfile.txt", "w");
    
    if(fptr == NULL){
        printf("error!");
        exit(1);
    }
    
    while(1){
        // printf("In a while!\n");
        //sleep(2000);
        fprintf(fptr, "%d", num);
    }
    fclose(fptr); */
    while(1){} 
    
    
    return 0;
}
